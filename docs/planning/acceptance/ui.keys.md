BOW code: MOD-011

# Acceptance criteria — ui.keys (MOD-011)

**BOW code:** MOD-011
**Spec refs:** UI-SPEC §3 (`docs/METROPOLIS-MASTER-v2.1.md` lines 742-756: leader-key grammar, which-key HUD, counts/repeat, marks/search, command palette, globals, mouse-optional); UI-SPEC §6 (line 780: "a layout/profile JSON schema (keymaps are remappable; the grammar above is the default binding set, stored as data)"); UI-SPEC §5 (performance budget — keystroke echo <10ms, pane focus <5ms, applicable to key handling latency); code.json `ui.keys` entry (inbound `KeyGrammar`, "Go package API + keymap JSON schema", pattern "leader-key state machine; every action registered with mnemonic path").
**Date:** 2026-08-10 (refreshed post-security-sweep; supersedes the 2026-08-08 draft; timeout/abort + prefix-ambiguity pass same day — see "What changed")
**Status:** active — `MOD-009` (ui.core) is `done`; this item is in the Sprint 2 `ready` queue (`node claude-bow.js ready`) with no open blocking dependency. It also discharges **FEAT-033** (`feat.skeleton key-input regression via ui.keys`, covers FEAT-006 AC-5b) — see AC-20/AC-21 below, added for that purpose.
**Package under test:** `internal/ui/keys/` (path from `node claude-bow.js show MOD-011`)
**Standard gates:** see `README.md` — all apply, package for SG-4/SG-7 is `./internal/ui/keys/...`.

## What changed since the 2026-08-08 draft

- **FEAT-033 must be discharged here, not left orphaned.** `feat.skeleton`'s (FEAT-006) AC-5 was split at Sprint 1 close: AC-5a (Command→Delta→render) shipped against real components; AC-5b (real key events reaching `Commands`) was explicitly deferred to this item, tracked as FEAT-033, with a standing rule recorded in `feat.skeleton`'s own criteria that **nobody may translate key events into Commands inside `cmd/metropolis` or `ui.core`** — that translation is this package's grammar, and duplicating it elsewhere violates GR#20/GR#3. AC-20/AC-21 below are new, added specifically to close FEAT-033 rather than leave it a dangling BOW item once this file goes `active`.
- **Weakness pattern #1** applies to this item's own AC-9 (palette lists "every registered action") and the mnemonic-path registry generally — AC-2b below adds the "cannot be broken through the public API" criterion.
- **Weakness pattern #4** applies to two trust boundaries this package didn't have BA attention on before: the remappable keymap JSON profile (AC-11) is user-editable data reaching a **dispatch decision** (which action a keystroke resolves to) and the palette's parameterised command syntax (AC-9, `:loan 5M 10y`) parses free text into **typed arguments** that downstream engine commands will consume — both get a named-domain, reject-not-repair treatment below (AC-11b, AC-9b).
- **Leader-sequence timeout/abort and prefix-ambiguity resolution were missing from the prior pass** (caught on a follow-up read against the dev-team-process assumption-logging duty). UI-SPEC §3 states `Esc` backs out one level generally but doesn't say so explicitly for a mid-sequence leader prefix, and doesn't name an inactivity timeout at all; neither did the original AC set. Added: AC-2c (explicit `Esc` abort of a pending sequence, distinguishable from dispatch), AC-2d (inactivity timeout auto-aborting the same way, injectable-clock only — no blocking sleep on T-INPUT's non-blocking contract), and AC-14b (a path cannot be both a registered action and a prefix of another registered path — the ambiguity AC-14's same-path duplicate rule doesn't reach). Logged as **ASM-117** (timeout duration is an implementer default, not spec-mandated) and **ASM-118** (reject-at-Register is the BA's chosen resolution for prefix/complete ambiguity; a wait-out-the-timeout alternative is plausible and is Bill's call if preferred).

## User stories

- **US-1.** As the player, I need a verb→noun→variant leader-key language (`b r s` = build road street) so that two to four keystrokes reach every command and thirty learned keys unlock three hundred (UI-SPEC §3).
- **US-2.** As a novice player, I need a which-key HUD that appears within one UI tick after any prefix and shows my available continuations, so that I can discover the grammar by reading rather than memorising documentation (UI-SPEC §3).
- **US-3.** As an expert player, I need the which-key HUD to dim after N uses of a learned sequence, vim-style counts/repeat (`5 b r s`, `.`), 12 map marks, and `/` name search, so that the UI withdraws as I learn and I can operate at the speed of thought (UI-SPEC §3).
- **US-4.** As the player, I need a fuzzy command palette (`:`) that lists every action with its key sequence printed beside it and accepts parameterised commands (`:loan 5M 10y`), so that the palette doubles as the tutorial for the key grammar (UI-SPEC §3, the "VS Code trick").
- **US-5.** As a player who wants a different physical layout, I need keymaps stored as a remappable JSON profile, so that the shipped grammar is the default binding set, not a hardcoded one (UI-SPEC §6).
- **US-6.** As every other UI screen/module built later in the sprint plan (e.g. `feat.saveux`, which depends on this item per code.json), I need a stable `KeyGrammar` package API — every action registered with a mnemonic path — so that new screens plug into one grammar rather than each inventing key handling.
- **US-7 (new — discharges FEAT-033).** As `feat.skeleton`'s deferred AC-5b, I need a real key event (not a synthetic/scripted one) to travel from a `tcell.EventKey`-shaped input through this package's grammar into a genuine `protocol.Command`, so the walking skeleton's "real key events reaching Commands" claim is proven against actual code rather than left as a tracked-but-unverified BOW item.
- **US-8 (new — weakness pattern #4).** As the player, I need my remapped keymap JSON profile and my typed palette commands to be validated against a positively-stated domain before they can influence which action fires, so a malformed or hostile profile/command string is rejected rather than silently resolving to the wrong action or crashing the input loop mid-session.

## Scope

The leader-key state machine, which-key HUD data model, counts/repeat/undo-redo hooks, marks, name search, fuzzy command palette, global bindings, and the remappable keymap JSON schema. Rendering the HUD/palette panes themselves belongs to `ui.core`/the F-screens; this item owns the grammar engine and the data it drives those panes with.

## Acceptance criteria

### Functional

- **AC-1 (GR#20).** A `KeyGrammar` (or equivalent) interface/type exists implementing the inbound contract from code.json: a leader-key state machine where every action is registered with a mnemonic path (e.g. `["b","r","s"]`). Check: `go doc ./internal/ui/keys KeyGrammar` shows a `Register(path []string, action Action)` (or equivalent) method and a `Feed(key)`/`HandleKey` method advancing the state machine.
- **AC-2.** Feeding the key sequence for a registered mnemonic path (e.g. `b`, `r`, `s`) invokes the registered action exactly once, and an incomplete prefix (`b`, `r`) invokes nothing yet. Check: a passing test asserts both (`grep -rn "func Test.*[Ll]eader\|func Test.*[Mm]nemonic" internal/ui/keys/*_test.go`).
- **AC-2b (weakness pattern #1 — invariant enforced, not stated).** "Every action registered with a mnemonic path" (the inbound-contract pattern, code.json) is not just documentation — verify the public API cannot be misused to dispatch an action that was never `Register`ed, or to advance the state machine into invoking one path's action while `Continuations()` reported a different set. Check: `go doc ./internal/ui/keys` shows no exported way to invoke an `Action` other than through a completed, registered mnemonic path (no exported `Dispatch(action Action)` bypass taking an arbitrary unregistered `Action`); a passing test asserts feeding a sequence that was never registered invokes nothing and returns a distinguishable "no such sequence" result rather than silently falling through to the nearest registered prefix (`grep -rn "func Test.*[Uu]nregistered.*[Ss]equence\|func Test.*[Nn]oSuchPath" internal/ui/keys/*_test.go`).
- **AC-2c (leader-sequence abort — UI-SPEC §3 "Esc backs out one level, never losing where you were").** Feeding `Esc` while a leader sequence is pending (any non-empty prefix state) aborts the in-progress sequence back to idle without invoking any action, and does so distinguishably from a completed sequence's dispatch (the caller/HUD can tell "aborted" from "no continuations exist"). A second `Esc` from idle is a no-op at this package's level (it is `ui.core`/the screen's job to interpret idle-state `Esc` as "back out a pane level" per UI-SPEC §3's `Esc`/`Esc-Esc-Esc` pane-navigation rule — out of scope here, see Scope). Check: `go doc ./internal/ui/keys` shows an `Abort()`/`Esc`-bound reset of pending state; a passing test drives a partial prefix, feeds `Esc`, then asserts (a) no action fired and (b) the state machine reports idle/no-pending-prefix afterward (`grep -rn "func Test.*[Aa]bort\|func Test.*[Ee]sc" internal/ui/keys/*_test.go`).
- **AC-2d (leader-sequence timeout — ASM-117).** A pending leader sequence has an inactivity timeout: if no further key arrives within a configurable duration, the state machine auto-aborts to idle exactly as AC-2c's explicit `Esc` does (same distinguishable "aborted, not dispatched" signal), rather than waiting indefinitely for a keystroke that may never come. The grammar must not implement this by blocking or sleeping on its own goroutine (UI-SPEC §1/§5's non-blocking T-INPUT contract) — an injectable clock/ticker the caller drives (the pattern `foundation.errors` already established for time-based logic, per AC-16) is required, not `time.Sleep`. Check: `go doc ./internal/ui/keys` documents the timeout mechanism and its injectable-clock seam; a passing test advances a fake clock past the threshold mid-sequence and asserts auto-abort to idle with no action dispatched (`grep -rn "func Test.*[Tt]imeout\|func Test.*[Ii]dleAbort" internal/ui/keys/*_test.go`). UI-SPEC §3/§6 do not name a duration, so the default is an implementer choice per GR#15 (documented, not hardcoded silently) — flagged as **ASM-117**, mirroring the AC-4 dim-threshold/AC-9 fuzzy-algorithm precedent already escalated to Bill below.
- **AC-3.** After any prefix keystroke, the grammar exposes the set of valid continuations (for the which-key HUD to render) via a queryable method, e.g. `Continuations() []Continuation`, populated within the same call — no async delay that would miss UI-SPEC §3's "within one UI tick" requirement structurally. Check: `go doc ./internal/ui/keys Continuations` (or equivalent) exists; a passing test asserts continuations are available synchronously after a prefix key (`grep -rn "func Test.*[Cc]ontinuation\|func Test.*[Ww]hichKey" internal/ui/keys/*_test.go`).
- **AC-4.** A per-sequence usage counter drives HUD dimming: after a configurable N uses of the same completed sequence, a query (e.g. `ShouldDimHUD(path)`) returns true. Check: a passing test drives a sequence N+1 times and asserts the dim threshold flips (`grep -rn "func Test.*[Dd]im" internal/ui/keys/*_test.go`).
- **AC-5.** Numeric count prefixes are supported: feeding `5`, `b`, `r`, `s` invokes the `b r s` action with a `Count == 5` (or equivalent) parameter, and no digit prefix defaults to `Count == 1`. Check: a passing test covers both counted and uncounted invocation (`grep -rn "func Test.*[Cc]ount" internal/ui/keys/*_test.go`).
- **AC-6.** `.` repeats the last build/zone action invoked (with its original count and target-relative parameters), and `u`/`U` invoke registered undo/redo hooks where the consuming screen/engine has registered them as available — the grammar calls through to a registered `Undo`/`Redo` capability, it does not implement undo semantics itself (that is the engine's "where the engine permits" per UI-SPEC §3). Check: `go doc ./internal/ui/keys` shows a `RepeatLast`/`.`-bound handler and `RegisterUndo`/`RegisterRedo` (or equivalent) hooks; passing tests cover repeat and undo/redo dispatch (`grep -rn "func Test.*[Rr]epeat\|func Test.*[Uu]ndo" internal/ui/keys/*_test.go`).
- **AC-7.** Twelve map marks are supported: `m a`..`m l` (or equivalent 12-slot addressing) records a location, `' a`..`' l` retrieves it; a 13th distinct mark identifier is rejected as invalid rather than silently overwriting mark `a`. Check: a passing test asserts exactly 12 valid mark slots and rejection of a 13th (`grep -rn "func Test.*[Mm]ark" internal/ui/keys/*_test.go`).
- **AC-8.** `/` opens a name-search mode over a caller-supplied searchable name index (the grammar doesn't own auto-naming data, it consumes it), and `n`/`N` step forward/backward through matches. Check: `go doc ./internal/ui/keys` shows a `Search(query string, index NameIndex)` (or equivalent) and `NextMatch`/`PrevMatch`; passing test coverage (`grep -rn "func Test.*[Ss]earch" internal/ui/keys/*_test.go`).
- **AC-9.** A fuzzy command palette exists that lists every registered action with its full mnemonic path rendered beside it, ranks matches by fuzzy score against free-text input, and accepts parameterised command syntax (e.g. `:loan 5M 10y` parses into an action name plus positional/typed arguments). Check: `go doc ./internal/ui/keys Palette` (or equivalent) exists; a passing test asserts a parameterised command parses into the expected action+args tuple (`grep -rn "func Test.*[Pp]alette" internal/ui/keys/*_test.go`).
- **AC-9b (weakness pattern #4 — palette free text is input).** The palette's free-text command string crosses a trust boundary the instant it stops being "text a human typed to be shown back" and starts being "instruction that resolves to an action plus typed arguments" (the privileged position it reaches). The allowed domain is stated positively per action-parameter type (e.g. a money argument is `^\d+(\.\d+)?[KkMm]?$` parsed into a typed `Money` value; a duration argument is `^\d+[ydwm]$`; free text is never passed through to a downstream `protocol.Command` payload field unparsed) and a string that doesn't match is **rejected** with a palette-visible error naming which argument failed and the expected form — never silently coerced to a zero value or truncated to "the parseable prefix". Check: `go doc ./internal/ui/keys` documents the positive grammar per typed argument kind; a passing test asserts `:loan abc 10y` (a non-numeric amount) is rejected with a named-argument error, not silently parsed as `0` (`grep -rn "func Test.*[Pp]alette.*[Ii]nvalid\|func Test.*[Rr]ejectArg" internal/ui/keys/*_test.go`).
- **AC-10.** Global bindings (`Space` pause, `1/2/3` speed, `o`/`O` overlay cycle, `?` context help, `!` top alert, `F9` ticker, `` ` `` console) are registered distinctly from the leader-sequence tree and always resolve regardless of current prefix state (a global fires even mid-sequence, or the grammar defines and documents the interruption rule if it doesn't). Check: `grep -n "Global\|globals" internal/ui/keys/*.go` finds a distinct registration path; a passing test asserts a global fires during an in-progress leader sequence, or `doc.go` documents that globals are blocked mid-sequence and why (`grep -rn "func Test.*[Gg]lobal" internal/ui/keys/*_test.go`).
- **AC-11.** Keymaps are loadable from a JSON profile file conforming to a documented schema, remapping physical keys to mnemonic paths without recompiling; the shipped default profile reproduces exactly the UI-SPEC §3 grammar (`b`→build, `z`→zone, etc.). Check: a `data/keymap-default.json` (or equivalent path) exists and validates against the schema; a passing test loads it and asserts at least the `b`, `z`, `p`, `s`, `d`, `i`, `g`, `t` top-level verbs are present (`grep -rn "func Test.*[Kk]eymap\|func Test.*[Dd]efault" internal/ui/keys/*_test.go`).
- **AC-11b (weakness pattern #4 — a remapped key is a security-adjacent decision, not just UX).** A keymap profile's physical-key-to-mnemonic-path mapping is treated as input reaching a **dispatch decision** (the privileged position it reaches: which `Action` a keystroke resolves to) the moment it is loaded from anywhere other than the shipped default file — e.g. a user profile directory, an imported/shared profile. The allowed domain is stated positively: every mapped key must resolve to a mnemonic path that names an *already-registered* action (never a free-form string later interpreted as a shell/OS command, a file path, or anything executed outside the `KeyGrammar` dispatch seam), and a profile entry mapping to an unregistered path is **rejected** for that entry specifically (not the whole profile silently discarded, and not silently ignored — AC-13 already requires the fallback-to-default behaviour for a malformed *file*; this AC covers a well-formed file with a semantically invalid entry). Check: `go doc ./internal/ui/keys` documents that a loaded mapping target is checked against the live action registry, never treated as executable outside it; a passing test loads a profile mapping a key to a nonexistent mnemonic path and asserts that specific binding is rejected/reported while the rest of the profile still loads (`grep -rn "func Test.*[Kk]eymap.*[Uu]nknownAction\|func Test.*[Ii]nvalidBinding" internal/ui/keys/*_test.go`).
- **AC-12.** Every mouse-triggerable action registered through the grammar also has a key path (UI-SPEC §3: "every mouse act has a key path") — this is enforced structurally by having mouse handlers invoke the same registered `Action` as their key-path equivalent, not a separate code path. Check: `grep -n "Action\b" internal/ui/keys/*.go` shows mouse-event handlers (if implemented in this package) dispatching through the same `Action` type as key handlers; if mouse handling lives in `ui.core` instead, `doc.go` states that this package's registered-action model is the shared seam both key and mouse paths call into.

- **AC-20 (discharges FEAT-033; feat.skeleton AC-5b).** A real `tcell.EventKey` (not a scripted/synthetic key sequence — that is `ui.harness`'s (MOD-014) job, a distinct consumer) fed into this package's key-handling entry point (the T-INPUT integration seam UI-SPEC §1 describes) resolves, for at least the walking-skeleton's boot key sequence, into a genuine `protocol.Command` sent on a real `Transport` — not a mock, not a translation layer living in `cmd/metropolis`/`ui.core` (the standing rule from `feat.skeleton`'s own criteria: **that translation belongs to `ui.keys`'s grammar, and duplicating it elsewhere violates GR#20/GR#3**). Check: `go doc ./internal/ui/keys` shows the `tcell.EventKey`→`Action`→`protocol.Command` path is complete within this package (or explicitly documents the one integration call `cmd/metropolis` makes to invoke it, with no logic duplicated there); a passing test drives a real `tcell.EventKey` sequence through this package and asserts a real `protocol.Command` is constructed and sent on a `Transport` (`grep -rn "func Test.*[Ff]eat006\|func Test.*[Ss]keleton\|func Test.*[Ee]ndToEnd" internal/ui/keys/*_test.go`).
- **AC-21 (discharges FEAT-033; the standing-rule guard).** No package outside `internal/ui/keys` contains its own key-event-to-`Command` translation logic — `grep -rn "protocol\.Command{" internal/ui/core/*.go cmd/metropolis/*.go` (excluding test files) finds no site constructing a `protocol.Command` directly from a raw `tcell.EventKey` without going through this package's registered-action dispatch. Check: the grep above returns no matches; if `cmd/metropolis`/`ui.core` need to construct a `Command` for a reason unrelated to key input (e.g. a boot-time synthetic command), `doc.go` distinguishes that case from the key-input path this AC guards.

### Error handling

- **AC-13 (GR#7).** Loading a malformed or schema-invalid keymap JSON profile produces a registry-sourced error (new `MET-U`-range code added to `data/errors.json`) identifying the offending key/path, and falls back to the shipped default profile rather than leaving the grammar unusable. Check: `grep -n "MET-" internal/ui/keys/*.go` finds a registry code reference; a passing test covers malformed-JSON load with fallback (`grep -rn "func Test.*[Mm]alformed\|func Test.*[Ii]nvalid.*[Kk]eymap" internal/ui/keys/*_test.go`).
- **AC-14.** Registering two actions at the same mnemonic path (a conflicting binding) is rejected at `Register` time with a typed error, never a silent last-write-wins overwrite that would make key grammar order-dependent. Check: passing test coverage (`grep -rn "func Test.*[Cc]onflict\|func Test.*[Dd]uplicate" internal/ui/keys/*_test.go`).
- **AC-14b (prefix/complete-binding ambiguity — ASM-118).** A mnemonic path that is simultaneously a registered action's complete path *and* a strict prefix of another registered path (e.g. `b` bound to an action while `b r` is also registered) is rejected at `Register` time with a typed error naming both conflicting paths, for the same reason AC-14 rejects same-path duplicates: without this rule the state machine cannot tell, on feeding `b`, whether to dispatch immediately or wait for a possible continuation, and UI-SPEC §3 doesn't specify a wait-vs-fire tiebreak — the grammar avoids the ambiguity structurally rather than picking an arbitrary rule for it. Check: `go doc ./internal/ui/keys` documents that a path cannot be both terminal and a prefix; a passing test registers `b` as complete and then attempts to register `b r`, asserting the second `Register` call fails with an error naming the conflict (`grep -rn "func Test.*[Pp]refix.*[Cc]onflict\|func Test.*[Aa]mbiguous" internal/ui/keys/*_test.go`). Flagged as **ASM-118**: UI-SPEC §3 doesn't address this case directly (every worked example, `b`/`b r`/`b r s`, has the leaf as the only terminal), so "reject at Register time" is the BA's chosen resolution — the alternative (allow it, fire the shorter path immediately and drop the longer one from reachability, or wait out a timeout before firing the shorter path) is a plausible reading Bill may prefer instead; either way it must be a documented, tested rule rather than an accident of implementation order.

### Determinism & safety

- **AC-15 (GR#21).** Feeding an identical key sequence against an identical registered-action set and keymap produces an identical resulting action+args dispatch every time — no randomness, no map-iteration-order dependence in continuation ordering (which-key HUD listings must render in a stable order). Check: `grep -rn "for .* := range" internal/ui/keys/*.go` — any range over a `map` feeding `Continuations()`'s output order is sorted before return (`grep -n "sort\." internal/ui/keys/*.go` corroborates); a passing determinism test exists (`grep -rn "func Test.*[Dd]eterminis\|func Test.*[Oo]rder" internal/ui/keys/*_test.go`).
- **AC-16 (SG-7 scoped; GR#21).** `grep -rn "time.Now\|time.Since" internal/ui/keys/*.go` (excluding `_test.go`) returns no matches outside a clearly-labelled, injectable-clock site used only for the HUD's own dim-after-N-uses bookkeeping (if time-based rather than count-based) or the 300ms highlight-pulse timing — any such site must be overridable for tests, per the pattern established in `foundation.errors`.
- **AC-17.** `go test ./internal/ui/keys/... -race -count=1` passes with no data race — the grammar's state machine must be safe if fed from the same T-INPUT goroutine UI-SPEC §1 describes while a concurrent palette search reads the action registry. Check: `grep -n "go func()" internal/ui/keys/*_test.go` finds at least one concurrency test.

### Documentation

- **AC-18.** `internal/ui/keys/doc.go` states the module key `ui.keys`, cites UI-SPEC §3 and §6, and documents the mnemonic-path convention (verb→noun→variant) new screens must follow when registering actions. Check: `grep -n "ui.keys" internal/ui/keys/doc.go` and `grep -n "UI-SPEC" internal/ui/keys/doc.go` both match.
- **AC-19.** The keymap JSON schema is documented (field names, types, how a remapped profile overrides the default) in `doc.go` or a sibling `.md`, since it is also `feat.saveux`'s and any future settings-screen's data contract. Check: file exists and is referenced by the loader code (AC-11).

## Out of scope

- Rendering the which-key HUD strip, palette pane, or search-match highlighting — that is `ui.core`/the relevant F-screen's job; this item exposes the data (`Continuations()`, `Palette` matches, search results) those panes render.
- The actual gameplay action set (`BuyLand`, zone/build commands) — those are registered into this grammar by the owning engine/UI modules as they go real (M0-ENG §2), not defined here.
- Auto-naming of objects (`§20`) that `/` search indexes against — this item only consumes a `NameIndex` interface, it does not generate names.
- Mouse drag-to-draw gestures for roads/zones — UI-SPEC §3 lists this as a `ui.core`/screen-level interaction; this item's mouse concern is limited to ensuring the shared `Action` dispatch seam (AC-12).

## Out of scope (addendum)

- Scripted/synthetic key-sequence DSL testing (`"b r s"`-as-string harness driving) — that is `ui.harness`'s (MOD-014) job; AC-20's "real `tcell.EventKey`" requirement is deliberately the complementary half (real input, not scripted), so the two items' regression coverage doesn't overlap or gap.

## Escalations

- **Assumption flagged (per BA instructions §3).** This item depends on `MOD-009` (ui.core, Sprint 1, now `done`). If `ui.core`'s widget/pane API doesn't expose a place to render `Continuations()`/palette output, the which-key HUD and palette panes may need to ship as data-only in this item with rendering deferred to a follow-up UI-layer BOW item — the owning BA should confirm ui.core's actual surface at dispatch and adjust AC-3/AC-9's "rendered" language if needed. Logged as **ASM-075** (see below).
- **For Bill.** UI-SPEC §3 doesn't specify the exact HUD dim threshold N or the palette's fuzzy-match algorithm/library choice; AC-4/AC-9 are written to be satisfiable by any reasonable choice the junior documents, per GR#15 (validators derive from data, not hardcoded assumptions) — flagging in case Bill wants a specific N or library mandated rather than left to the implementer.
- **For Bill (new).** AC-20 discharges FEAT-033 by requiring this item's own tests to prove the real-key-event path end to end, rather than treating FEAT-033 as a separate follow-up item once `ui.keys` lands. This reads as the correct close per the build log's "defer with a tracked item, never drop and never pretend" doctrine (the same one that created FEAT-032/FEAT-033), but flagging since it means `ui.keys`'s own Tester verdict is also FEAT-033's closing verdict — Bill should `done` FEAT-033 with a ref to `ui.keys`'s commit rather than leaving it open as a phantom follow-up once AC-20/AC-21 pass.
- **Assumption flagged (v1.7 logging).** Logged as **ASM-117**: no UI-SPEC-mandated leader-sequence inactivity-timeout duration exists (AC-2d); left as an implementer default in the same spirit as AC-4's dim-threshold N and AC-9's fuzzy-match algorithm, per GR#15 ("validators derive from data, not hardcoded constants" — the point is the default is documented and testable, not that a specific number is invented here). If Bill wants a mandated duration (e.g. matching a HUD-appear budget), that's a spec call, not a BA judgment call.
- **Assumption flagged (v1.7 logging).** Logged as **ASM-118**: chose reject-at-`Register` as the resolution for a mnemonic path that is both a complete action and a prefix of a longer registered path (AC-14b), rather than a runtime wait-out-the-timeout tiebreak. UI-SPEC §3's worked examples never exercise this shape, so this is the BA's structural-avoidance choice, not a spec requirement — flagging for Bill in case a runtime tiebreak (fire short path after AC-2d's timeout elapses with no continuation keystroke) is preferred over a compile/register-time rejection.
- **Assumption flagged (v1.7 logging).** Logged as **ASM-076**: assumed the walking skeleton's real terminal input path already exists in a form this package can hook into (a `tcell.EventKey` source wired in `cmd/metropolis`, per `feat.skeleton`'s closed AC-1/AC-5a) — if `cmd/metropolis`'s actual boot wiring doesn't yet expose a call site for `ui.keys` to attach to, AC-20 needs re-scoping at dispatch to name the exact integration point rather than the illustrative "T-INPUT seam" language used here.
