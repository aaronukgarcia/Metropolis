BOW code: MOD-039

# Acceptance criteria — engine.refuse (MOD-039)

**BOW code:** MOD-039
**Spec refs:** §25 Refuse Collection & the Waste–Health Loop (`docs/METROPOLIS-MASTER-v2.1.md` lines 406-409: per-cell waste generation into bin stock — residential wheelie/commercial trade/industrial skip, differing capacities; scheduled rounds run by real trucks on real roads from depots/transfer stations, auto-optimised, player-overridable, consuming road space and fuel like any freight; missed collections — truck shortage, gridlock, strike event, depot underfunding — leave bin overflow → vermin index up → local physical health down, land value down, fire risk up, ticker names the street; streams: general→landfill/incinerator, recycling with player-set service level and contamination reducing resale, food waste→composting→farm input §31; landfills fill *permanently* and blight; incineration trades airshed pollution for energy; exhausted sites can be capped and reclaimed as parkland §32; "the loop: waste budget cuts are invisible for months, then vermin, then illness, then emigration — a classic Detroit on-ramp"); §31 Farming & the Biodiversity Engine (lines 441-448, the compost→farm-input consumer of the food-waste stream); code.json `engine.refuse` entry (inbound `RefuseAPI`, "rounds are logistics movements; overflow states per cell", consumers: `engine.farming`, `feat.compositionroot`; outbound calls `engine.logistics`, `engine.services`, `engine.wellbeing`, `engine.farming`, `engine.market`).
**Date:** 2026-08-11 (BA-S9a, first pass)
**Status:** draft-ahead — dependencies `MOD-025` (engine.logistics) and `MOD-033` (engine.services) are both BOW-status `done` (2026-08-16); this item itself is BOW-status `open` with no `internal/engine/refuse/` code on disk yet, so it is not in the ready queue until dispatched. BUG-058 is CLOSED (c36778b) — the `engine.refuse`↔`engine.farming` edge is registered both directions (AC-10 re-armed); the still-absent `engine.invariant` edge is governed by the collaborations gate, not "pending BUG-058" (see AC-12/Escalations).
**Package under test:** `internal/engine/refuse/` (path from `node claude-bow.js show MOD-039`)
**Standard gates:** see `README.md` — all apply, package for SG-4/SG-7 is `./internal/engine/refuse/...`.

## User stories

- **US-1.** As every occupied cell, I need waste generated each tick into a typed bin stock (wheelie/trade/skip) with a documented capacity, so overflow is a real state transition, not a flavour description.
- **US-2.** As `engine.logistics` (MOD-025, a registered outbound dependency), I need refuse rounds expressed as ordinary logistics movements (per code.json's "rounds are logistics movements" pattern) — trucks from depots on real roads, consuming junction slots and fuel like any freight — so refuse does not reimplement a second truck-routing model.
- **US-3.** As `engine.wellbeing` (a registered outbound dependency), I need overflow to feed the physical-health driver's pollution/vermin term through the registered `WellbeingAPI`, so the health consequence of a missed round is a live query, not a duplicated health table.
- **US-4.** As `engine.services` (a registered outbound dependency, MOD-033), I need refuse crews staffed from the Public Service Pie's refuse-crew benchmark ratio and refuse funding to gate round frequency/reliability through the generic service-funding→quality function, so a refuse budget cut is felt exactly like any other service cut.
- **US-5.** As the player watching the ticker, I need a missed collection to name the affected street, so the consequence is legible and locatable, not an aggregate city-wide number.
- **US-6.** As `engine.invariant` (MOD-019), I need refuse tonnage registered as a conserved stock the moment this module exists, so "conservation invariants extended to each new stock" (the S9 exit gate) is true of refuse specifically, not merely asserted.
- **US-7.** As `engine.farming` (§31, now a registered consumer — the `engine.refuse`↔`engine.farming` edge landed in c36778b), I need the food-waste→compost→farm-input chain to be a real, queryable output, not a described-but-inert flavour loop.

## Scope

Per-cell waste generation into typed bin stock; scheduled, auto-optimised, player-overridable collection rounds expressed as `engine.logistics` movements; missed-collection overflow states with the documented downstream chain (vermin → physical health → land value → fire risk, ticker-named); the three waste streams (general→disposal, recycling with contamination, food→compost); landfill/incineration disposal-site lifecycle (fill, blight, cap-and-reclaim per §32); mass-conservation accounting for refuse tonnage as a new invariant-checker stock.

## Acceptance criteria

### Functional — waste generation and bin stock

- **AC-1 (GR#20; code.json inbound contract).** A `RefuseAPI` exists exposing per-cell bin-stock queries (level, capacity, type) and round-scheduling/override commands, matching code.json's documented pattern "rounds are logistics movements; overflow states per cell". Check: `go doc ./internal/engine/refuse RefuseAPI` shows both a bin-stock query surface and a command surface for round overrides; `grep -n "func.*Override" internal/engine/refuse/*.go` confirms player overrides are commands, not direct field writes.
- **AC-2 (§25 typed bin stock).** Every occupied cell carries a bin stock typed by land use — residential wheelie, commercial trade bin, industrial skip — each with a distinct, documented capacity, and waste is generated into it each tick from the cell's §17-consumption-coefficient-derived waste rate. Check: `go doc ./internal/engine/refuse BinStock` (or equivalent) shows the three types with distinct capacity fields; a passing test asserts a residential and an industrial cell of identical population/output generate into stocks with different capacities (`grep -rn "func Test.*[Bb]inType\|func Test.*[Cc]apacity" internal/engine/refuse/*_test.go`).
- **AC-3 (§25 three streams).** General waste, recycling (with a player-set service level and a contamination factor that reduces recycling resale value), and food waste (routed to composting) are tracked as three distinct sub-stocks per cell, not one undifferentiated waste number split only at disposal time. Check: `go doc ./internal/engine/refuse` shows three named stream accessors; a passing test asserts raising the contamination input lowers the recycling stream's resale-value output while leaving the general and food streams unaffected (`grep -rn "func Test.*[Ss]tream\|func Test.*[Cc]ontamination" internal/engine/refuse/*_test.go`). **Lazy implementation this rejects:** a single blended "waste" number with a recycling percentage applied only at the point of disposal would satisfy "recycling exists" on a shallow read but couldn't isolate a contamination effect from a collection-rate effect — the check requires contamination to move only the recycling stream's own output.

### Functional — collection as logistics movements

- **AC-4 (§25; US-2; GR#20 outbound contract).** Scheduled collection rounds are literally `engine.logistics` movements — trucks depart depots/transfer stations, consume junction slots and fuel, and travel real roads — reusing `LogisticsAPI`'s existing stock-draw/movement machinery (per `engine.logistics.md` AC-8's "same underlying type/method set, not merely a similarly-named one" precedent) rather than a bespoke refuse-only truck model. Check: `grep -rn "logistics\.(Movement|Draw|Stock)" internal/engine/refuse/*.go` (excluding `_test.go`) shows refuse rounds constructed through `engine.logistics`'s exported movement type, not a parallel `RefuseTruck` type with independent scheduling logic; a passing test asserts a saturated junction on a refuse truck's route produces the same next-day-queue behaviour `engine.logistics.md` AC-4 defines for any other movement (`grep -rn "func Test.*[Rr]ound.*[Mm]ovement\|func Test.*[Tt]ruckQueue" internal/engine/refuse/*_test.go`). **False-pass risk:** a `RefuseTruck` type that merely looks similar (same field names) but is scheduled by independent code would pass a naming grep while never actually contending for the junction slots `engine.logistics` tracks — the check requires the saturation scenario to reproduce `engine.logistics`'s own queueing behaviour, proving genuine reuse.
- **AC-5 (§25 auto-optimisation, player-overridable).** Round routing/scheduling is auto-optimised by default (a documented objective, e.g. minimising total truck-distance or maximising on-time collection coverage) and the player can override a specific round's route/schedule via a command that takes precedence over the auto-optimiser for that round until cleared. Check: `go doc ./internal/engine/refuse` shows an auto-optimise entry point and an `OverrideRound` command; a passing test asserts an overridden round's actual route differs from the auto-optimiser's output and persists across the next optimisation pass until explicitly cleared (`grep -rn "func Test.*[Oo]verride" internal/engine/refuse/*_test.go`).
- **AC-6 (§25 missed collection causes).** A round can fail to complete (truck shortage from the refuse-crew staffing pool, gridlock delay exceeding the round's time window, a strike event, or depot underfunding), and each cause is independently constructible in a test — a single generic "collection failed" boolean is insufficient because the spec names four distinct causes with (implicitly) distinct downstream framing in the ticker. Check: `go doc ./internal/engine/refuse` shows a typed miss-cause field/enum with at least the four named values; a passing test constructs each cause independently and asserts the resulting bin-stock overflow state records which cause applied (`grep -rn "func Test.*[Mm]issedCollection" internal/engine/refuse/*_test.go`).

### Functional — overflow chain and disposal lifecycle

- **AC-7 (§25 overflow chain, the causal spine of this module).** A cell whose bin stock exceeds capacity (missed collection with continued generation) enters a documented overflow state that drives, in order: a vermin index (rising with overflow duration/magnitude), then a physical-health penalty routed through `WellbeingAPI`'s registered `PollutionExposure`-or-equivalent driver (not a refuse-owned duplicate health number), a land-value penalty, and a fire-risk increase — and the ticker names the affected street. Check: `go doc ./internal/engine/refuse` shows the vermin-index/land-value/fire-risk accessors and a street-naming ticker-event type; a passing test drives one cell into sustained overflow and asserts all four consequences move in the documented direction, with the wellbeing consequence specifically asserted via a call into the registered `WellbeingAPI` (not a locally reimplemented health formula) (`grep -rn "func Test.*[Oo]verflow.*[Cc]hain\|func Test.*[Vv]erminIndex" internal/engine/refuse/*_test.go`). **Lazy implementation this rejects:** a build that computes its own "overflow health penalty" number independent of `WellbeingAPI` would satisfy "health goes down" on a shallow read while duplicating a system `engine.wellbeing` already owns (GR#3 single-source-of-truth) — the check requires the call to cross the registered interface.
- **AC-8 (§25 landfill permanence and blight; §32 reclamation).** A landfill site's remaining capacity only ever decreases (general waste routed there is permanently consumed capacity, never reclaimed by simply stopping input), a full landfill triggers a documented blight effect on its surrounding cells (reusing §32's reclamation/blight mechanic per the spec's explicit "§32 reclamation shares the mechanic" cross-reference, not a refuse-local reimplementation), and a capped, exhausted site can transition to reclaimed parkland via a command, after which it is no longer a valid disposal target. Check: `go doc ./internal/engine/refuse` shows a landfill-capacity accessor that only decreases under load and a `CapAndReclaim` (or equivalent) command; a passing test fills a landfill to capacity, asserts blight applies to a neighbouring cell, then reclaims it and asserts routing further general waste there is rejected (`grep -rn "func Test.*[Ll]andfill.*[Bb]light\|func Test.*[Rr]eclaim" internal/engine/refuse/*_test.go`).
- **AC-9 (§25 incineration trade-off).** Routing general waste to incineration instead of landfill produces energy output (queryable, feeding whatever energy-accounting module later consumes it) at the documented cost of an airshed-pollution term, distinct from and in addition to the overflow-driven `PollutionExposure` term of AC-7 — so a player choosing incineration over landfill faces a real trade-off, not a strictly-dominant option. Check: `go doc ./internal/engine/refuse` shows both an energy-output accessor and an airshed-pollution accessor on the incineration path, with no equivalent airshed term on the landfill path; a passing test asserts routing identical waste volume to incineration vs. landfill produces different pollution profiles (`grep -rn "func Test.*[Ii]ncinerat" internal/engine/refuse/*_test.go`).
- **AC-10 (§31 food-waste→compost→farm-input chain — RE-ARMED 2026-08-11, BUG-100: the `engine.refuse`↔`engine.farming` edge landed in c36778b, both directions).** The food-waste stream's collected tonnage becomes a queryable compost output that is a genuine input value AND is now legally consumable by `engine.farming` — `node -e "const m=require('./code.json').modules.find(x=>x.key==='engine.farming'); process.exit(m.outbound.calls.some(c=>c.key==='engine.refuse')?0:1)"` exits 0, confirming the edge (see `engine.farming.md` AC-10, the mirror of this check). This AC requires the compost output to be a real, independently queryable, exported value on `RefuseAPI` — a build that tracks compost tonnage as an internal-only, unexported number satisfies neither GR#3 nor this AC's intent — and, now the edge exists, `engine.refuse`'s own side of the doc.go/Escalations block citation must be removed (it is `engine.farming`'s consumption call that completes the wiring, per that file's AC-10; this file's obligation is only to expose the accessor, which no longer carries a "blocked" caveat). Check: `go doc ./internal/engine/refuse` shows an exported compost-output accessor; a passing test asserts food-waste tonnage collected converts to compost output at a documented (data-sourced, GR#15) ratio (`grep -rn "func Test.*[Cc]ompost" internal/engine/refuse/*_test.go`); `internal/engine/refuse/doc.go` no longer cites BUG-058 as a live block for AC-10.

### Functional — conservation identity (the mass-conservation stock)

- **AC-11 (mass conservation — the strongest available check, per the brief).** For every accounting period (tick or month, documented which), the following identity holds exactly for every waste stream and every cell/city-wide aggregate the module tracks: `TonnesGenerated == TonnesCollected + TonnesUncollected(overflow) + TonnesInTransit(en-route in a logistics movement, per AC-4) + TonnesDisposalBacklog(queued at a landfill/incinerator/compost facility awaiting processing)`. No term may be inferred as a remainder — each is independently computed from its own source (generation from §17 coefficients, collected from completed round deliveries, uncollected from bin-stock overflow state, in-transit from `engine.logistics`'s own movement ledger per AC-4, backlog from the disposal site's own queue) and then the identity is *checked*, not constructed to balance by definition. Check: a passing test runs a synthetic city for several ticks with a mix of successful and missed rounds, independently sums each of the four right-hand terms from its own source accessor, and asserts the sum equals the independently-tracked `TonnesGenerated` figure to an exact (integer-tonnage) or documented epsilon (`grep -rn "func Test.*[Mm]assConserv\|func Test.*[Tt]onnage.*[Ii]dentity" internal/engine/refuse/*_test.go`). **Lazy implementation this rejects:** computing `TonnesUncollected` (or any term) as `TonnesGenerated − (the other three)` rather than from its own independent source would make the identity trivially, tautologically true regardless of a bug in any individual term's real accounting — indistinguishable from correct until a downstream consumer (health, land value) reads a wrong number. The check requires all four terms sourced independently before summing, exactly analogous to `engine.wellbeing.md`'s AC-2/AC-3 pairing (additive identity alone is insufficient; independence of the terms is what makes the identity able to fail).
- **AC-12 (US-6; registration with `engine.invariant` — STILL BLOCKED after c36778b; BUG-100 tripwire applied).** Refuse tonnage (per AC-11's identity) is registered as a conserved stock with `engine.invariant`'s (MOD-019) registry, per that module's documented "registration seam for adding new conserved stocks" (`engine.invariant.md` US-5), so a bug that silently creates/destroys tonnage is caught by the standing invariant-checker infrastructure every tick, not only by this package's own test suite. **Blocked — edge still absent (BUG-058 closed, c36778b):** code.json still shows (a) no `engine.refuse`→`engine.invariant` outbound call edge — c36778b registered `engine.refuse`↔`engine.farming`, not this edge — and (b) `engine.invariant`'s own inbound contract is populated only by `engine.citizens`/`engine.finance`/`engine.traffic`, `engine.refuse` still absent. The edge is now governed by the collaborations gate (declare `engine.invariant` in this module's `collaborations.consumesFrom` in `master-plan-v2.1.json` to have `generate.js` enforce it), rather than "pending BUG-058". The package must still pass AC-11's local identity test (which needs no cross-module edge) as the interim proof of correctness. **Tripwire (mechanical, not a prose label — BUG-100):** `node -e "const m=require('./code.json').modules.find(x=>x.key==='engine.refuse'); process.exit(m.outbound.calls.some(c=>c.key==='engine.invariant')?1:0)"` must exit **0** (edge still absent). A nonzero exit means the edge has landed and this AC is stale, unarmed prose that MUST be rewritten to the real check before the next commit touching `internal/engine/refuse/`; Tester/CI treat a nonzero exit here as a hard FAIL, not a silent pass. Until the tripwire fires: once unblocked, `grep -n "invariant\.Register" internal/engine/refuse/*.go` shows a registration call at package init/boot; until then, `internal/engine/refuse/doc.go` states the block (edge absent) explicitly, noting BUG-058 is closed and the edge is governed by the collaborations gate.

### Error handling

- **AC-13 (GR#7).** Scheduling a round against an unregistered depot ID, or querying bin stock for a cell with no registered land-use type, returns a registry-sourced error (new `MET-E`-range code, claimed against `data/errors.json`'s reserved table at build time — no `engine.refuse` sub-range exists yet) rather than a silently-created zero-value stock entry. Check: `grep -n "MET-" internal/engine/refuse/*.go` finds a registry code reference; passing test coverage (`grep -rn "func Test.*[Uu]nregisteredDepot\|func Test.*[Uu]nknownCell" internal/engine/refuse/*_test.go`) — **GR#7 assertion, stated explicitly (BUG-100):** the test asserts the returned error's registry code matches AND that no zero-value stock entry was created, not merely that a matching-named test function exists.
- **AC-14.** A round-override command targeting a round that has already completed, or a negative/over-100% contamination or funding input, is rejected with a typed error, never silently clamped or silently ignored. Check: passing test coverage (`grep -rn "func Test.*[Ii]nvalidOverride\|func Test.*[Oo]utOfRange" internal/engine/refuse/*_test.go`).

### Determinism, safety & scale

- **AC-15 (GR#21).** Round auto-optimisation, overflow accumulation, and disposal-site allocation are deterministic functions of `(worldSeed, tick, prior state, commands)` — repeated runs from identical starting state and command sequence produce byte-identical bin-stock levels, overflow states, and landfill capacities, across worker counts. Check: `grep -rn "func Test.*[Dd]etermnis\|func Test.*[Dd]eterminis" internal/engine/refuse/*_test.go`.
- **AC-16 (SG-7; GR#21).** `grep -rn "time.Now\|time.Since" internal/engine/refuse/*.go` (excluding `_test.go`) returns no matches.
- **AC-17.** `go test ./internal/engine/refuse/... -race -count=1` passes; `grep -n "go func()" internal/engine/refuse/*_test.go` finds at least one concurrency test (concurrent round processing across depots within a tick).

### Documentation

- **AC-18.** `internal/engine/refuse/doc.go` states the module key `engine.refuse`, cites §25 and §31, states the mass-conservation identity (AC-11) in prose with all four independently-sourced terms named, states the `engine.invariant` registration block (AC-12) explicitly (edge absent; BUG-058 closed, now governed by the collaborations gate) so a future reader does not assume the registration already happened, and states the incineration energy/pollution trade-off (AC-9) so a future balance pass understands why incineration is not strictly dominant. Check: `grep -n "engine.refuse" internal/engine/refuse/doc.go` and `grep -n "§25\|section 25" internal/engine/refuse/doc.go` both match; `grep -n "collaborations\|BUG-058" internal/engine/refuse/doc.go` matches.

## Out of scope

- `engine.logistics`'s own movement-scheduling/queueing mechanics — this item only *drives* that machinery for refuse-typed movements (AC-4); it does not reimplement or extend it.
- `engine.wellbeing`'s own physical-health driver decomposition — this item only supplies the `PollutionExposure`-or-equivalent input via the registered interface (AC-7); the driver math itself belongs to `engine.wellbeing`.
- `engine.farming`'s consumption of compost output, and the compost-to-yield mechanic itself (§31) — out of scope here (the `engine.refuse`↔`engine.farming` edge is now registered; the mechanic itself is `engine.farming`'s, per that file's AC-10).
- `engine.invariant`'s own registry/hard-assert/release-error mechanics — this item only registers against it once the edge exists (AC-12); it does not build a second invariant framework.
- The specific waste-per-capita generation rate and bin-capacity figures — balance data pending M2 tuning (see Escalations).

## Escalations

- **Registry edges (refreshed 2026-08-16 — BUG-058 is CLOSED, c36778b).** (1) `engine.refuse`↔`engine.farming` is now registered both directions (AC-10 re-armed). (2) `engine.refuse`→`engine.news` remains absent — §25's "the ticker names the street" plausibly needs a path into `engine.news` (MOD-043); AC-7's street-naming requirement can still be satisfied by emitting a documented ticker-event value from `RefuseAPI` that `engine.news` can subscribe to once its own edge exists, so this remains a likely-needed edge rather than a hard blocker. (3) `engine.invariant`'s inbound contract is still `engine.citizens`/`engine.finance`/`engine.traffic` only — `engine.refuse`→`engine.invariant` still absent (AC-12). The still-absent edges are now governed by the collaborations gate (declare them in `master-plan-v2.1.json` to have `generate.js` enforce them), not "pending BUG-058".
- **For Aaron (balance numbers).** Bin capacities by land-use type, the waste-per-capita generation rate, contamination's resale-value penalty curve, and the vermin-index accumulation rate are none of them spec-given magnitudes — §25 describes the mechanism and direction but no numbers. ACs above check shape/direction only (per GR#15); the actual figures are `data/refuse.json` (or equivalent) balance data pending M2 tuning, same pattern as `engine.season.md`'s curves and `engine.logistics.md`'s buffer multipliers.
- **For Ben (dependency risk).** This item depends on `MOD-025` (engine.logistics) and `MOD-033` (engine.services), both `done` (2026-08-16) — no longer a dependency block. AC-4's reuse requirement (rounds ARE logistics movements, not a parallel type) remains the AC most exposed to `engine.logistics`'s concrete API shape; recommend the junior build against `engine.logistics`'s interface stub first, with the Tester re-verifying AC-4 once the real dependency lands, consistent with `engine.logistics.md`'s own escalation on the same pattern.
- **Assumption logged — ASM-1024 (P2, directional balance data).** `data/refuse.json`'s full figure set — bin capacities, waste rates, stream mix, contamination penalty, vermin rates, incineration energy/airshed, compost ratio, funding threshold, and truck capacity/crews — are all directional placeholders pending the M2 balance pass (balance-number regime, GR#15); none are spec-stated magnitudes.
- **ASM-1237 (assumption — balance-number placeholder).** The bi-weekly refuse collection cadence is a data-defined placeholder default (§25 names collection rounds with no cadence); the food-to-waste mass-conservation mapping must not bake in a cadence Aaron's balance pass owns.
- **ASM-1113 (ESCALATE, content-less).** Title is empty "MOD-039"; identified as engine.refuse. Nothing to fold. Leave open for manual triage.

## Spec-fold amendments (FEAT-084 SF wave, 2026-08-18)

> Substantive AC amendments folded from the FEAT-084 ASM disposition (class SF). Each entry names the source ASM, the AC it amends, and the amended requirement plus its check. Where an entry conflicts with earlier wording in this file, the entry is authoritative.

### ASM-1023 — AC-11 mass-conservation term boundaries (amends AC-11)
The accounting period for AC-11's identity is the **tick**, not the month. The four right-hand terms are defined precisely, each sourced independently from its own ledger and never inferred as a remainder: **TonnesCollected** is tonnage processed into terminal form (landfill fill, incineration, compost, recycling resale); **TonnesUncollected** is in-bin stock plus overflow; **TonnesInTransit** is the round-movement ledger (the `engine.logistics` movement in flight per AC-4); **TonnesDisposalBacklog** is the disposal-site queue awaiting processing. Check: the AC-11 test independently sums all four terms from their own accessors and asserts the identity holds exactly (integer tonnage), with each term's source named in the test.

### ASM-1044 — AC-11 identity test must drive TonnesInTransit nonzero (amends AC-11)
The AC-11 identity test must exercise a **nonzero TonnesInTransit** term, not only the zero-in-transit cases — a fully-delivered round and a strike miss both leave in-transit at 0. A bug that double-counts or drops in-transit tonnage is currently only caught by the separate AC-4 saturation test, not by the identity test itself. Check: the AC-11 test includes at least one scenario with a round in flight (partial delivery mid-tick) such that the in-transit term is independently observed nonzero and the identity still balances.

### ASM-1045 — AC-13 error range is MET-G1900–G1999, not MET-E (amends AC-13)
The E layer (E000–E999) is exhausted and the earlier G blocks are claimed by other engine modules, so `engine.refuse` opens **MET-G1900–G1999** under the BUG-234 code-format widening (codes MET-G1900–G1908 registered in `data/errors.json`). The AC-13 wording "new MET-E-range code" is superseded; the registry-sourced requirement (GR#7) is unchanged — only the layer letter differs. Check: `grep -n "MET-G19" internal/engine/refuse/*.go` matches, and the claimed codes are present in `data/errors.json`'s reserved table.

### ASM-1057 / ASM-1068 — direct-route primitives credit the generated term (amends AC-11)
`RouteGeneralToSite` (landfill + incinerator paths) and `RouteFoodToCompost` represent externally-sourced waste introduced into the accounting period, so they credit the **TonnesGenerated** counter by exactly the accepted tonnage alongside the **TonnesCollected** term. Without this, a caller that routes waste with zero `Generate` calls can never balance the AC-11 identity. Check: a test routes a fixed tonnage through each direct-route primitive with zero `Generate` calls and asserts `generated == collected` holds integer-exact on that path.

### ASM-1058 / ASM-1077 / ASM-1069 — Wire is idempotent and preserves landfill fill (amends AC-8)
`Wire` must not reset the provisioned disposal-site shelves when re-invoked. `Wire` only re-provisions shelves when the logistics instance pointer actually changes; a re-wire with the same instance leaves the provisioned map intact, and `ensureSiteShelf` carries any existing shelf level across a re-provision. Re-wiring therefore never re-increases `RemainingCapacity` nor clears blight on a full landfill. Check: a test fills a landfill to capacity, re-wires with the same logistics instance and then with a changed one, and asserts `RemainingCapacity` stays monotone non-increasing (AC-8) in both cases.

### ASM-1070 — RunRound propagates the funding-level error (amends AC-6)
When `services.FundingLevel(RefuseServiceID)` errors mid-round, `RunRound` returns the registry-sourced error rather than silently skipping the underfunding miss cause. An unreadable funding level is an error condition (GR#1/GR#7), not a legitimate `MissDepotUnderfunding` cause. Check: a test forces the funding-level query to error and asserts `RunRound` returns the registry error and records no miss-cause for that round.

### ASM-1090 — the general stream is the exact remainder (amends AC-3)
`streamMix.general` is removed from the data model and the general stream is computed as `generated − recycling − food` in `generate.go`, so the three per-tick stream allocations sum exactly to the generated whole-kg figure (the AC-11 source term). `RefuseStreamMix` carries only recycling and food; `data/refuse.json` carries no `general` key (a separately-configured general fraction would be dead data). Check: `grep -n "general" internal/engine/refuse/*.go` shows the general stream as a computed remainder, and `data/refuse.json` has no `general` key.

### ASM-1092 / ASM-1095 — ScheduleRound rejects re-scheduling an existing round (amends AC-5 / AC-14)
`ScheduleRound` was the only round mutator that did not reject an existing/completed round ID, so re-scheduling overwrote `roundState` with a fresh zero `inTransit`, silently dropping stranded tonnage and breaking the AC-11 identity. It now rejects any existing round ID with `ErrInvalidOverride` (reject, not carry-forward), matching the four sibling mutators (`AutoOptimise`/`OverrideRound`/`ClearOverride`/`RunRound`). Check: a test leaves tonnage in transit, re-schedules the same round ID, asserts `ErrInvalidOverride`, and asserts the AC-11 identity still holds with the in-transit term intact.

### ASM-1111 — disposal Register* reject duplicate siteID (amends AC-8/AC-9/AC-10/AC-11)
`RegisterLandfill`/`RegisterIncinerator`/`RegisterCompostSite` reject a duplicate `siteID` (consistent with `ScheduleRound` rejecting an existing round). The ACs nowhere require idempotent re-register of a disposal site; a site is registered once and reclaimed via `CapAndReclaim`, never re-opened. Check: a test registers the same `siteID` twice and asserts a typed reject, with the first registration's durable state preserved.

### ASM-1117 — AC-17 concurrent-mutation scope is the documented contract (amends AC-17)
The `*RefuseAPI` doc contract ("safe for concurrent use; every mutable field guarded by `mu`") makes concurrent `Wire`/`CapAndReclaim` alongside `RouteGeneralToSite`/`ProcessDisposal`/`RunRound` a **supported** contract, not out-of-contract misuse. Data races on `site.reclaimed` and `r.logistics`/`r.services` therefore violate that documented contract. Check: the AC-17 race test includes at least one concurrent-mutation scenario crossing a disposal-site write with a routing call, run under `-race`.

### ASM-1022 — AC-4 movement capacity is stub-depth logistics throughput (amends AC-4)
At `engine.logistics`'s stub-for-baseline depth (no junction slot ledger), the refuse round's movement capacity is `logistics` `Deliverable` throughput/shortfall — the stub analogue of the junction queue — and the landfill fill is a logistics stock shelf via `Restock`/`Stock`. A saturated throughput queues the shortfall to the next tick rather than teleporting. Check: the AC-4 saturation test reproduces `engine.logistics`'s own shortfall-queueing behaviour via `Deliverable` (not a parallel `RefuseTruck` type), and asserts the shortfall is deferred to the next tick.
- **ASM-1046 (confirm-and-close).** Gridlock-delay miss empties the bin into in-transit tonnage (next-tick queue semantic); strike/underfunding/truck-shortage leave bins full.
- **ASM-1183 (confirm-and-close).** Concurrent RunRound on the same round is rejected with ErrInvalidOverride via an unexported roundState.active flag, not blocked or queued.

- **ASM-1243 (confirm-and-close).** The dead `depsSnapshot.wellbeing` field was removed (captured in `snapshotDeps`, never consumed); `r.wellbeing` is read only inside `RefuseAPI.Generate` under `r.mu`, so it needs no depsSnapshot member.

## Confirm-and-close folds (FEAT-084 CC wave, 2026-08-20)

- **ASM-1026 (confirm-and-close).** Recycling resale has no disposal-site movement — collected as immediate resale on round delivery; general and food route through `engine.logistics` to their sites, and a fourth recycling-transport movement is a possible full-depth extension, not baseline scope.
- **ASM-1027 (confirm-and-close).** `engine.logistics` models waste as a single commodity, so the per-stream in-transit split lives in the refuse round ledger, not a logistics stock shelf; the three-stream distinction is refuse responsibility.
- **ASM-1096 (confirm-and-close).** `deliverToSite` now rejects a reclaimed (or unknown) site by returning `(0, tonnage)` so the round tonnage stays in-transit (gridlock-delay shortfall) and the caller can re-point `SetGeneralSite` and re-run — the refuse-level analogue of `RouteGeneralToSite`'s reclaimed rejection, living here because `logistics.Deliverable` has no refuse-reclaim concept.
- **ASM-1186 (confirm-and-close).** `ProcessDisposal` claims the full backlog before the external `Restock` and restores the un-accepted remainder on error; the full-landfill shortfall is `SatSub(general, added)` (equals `general - added` because `logistics.Restock` never returns `added > general` or negative).


### ASM-1184 — dependency snapshot is point-in-time (amends AC-17)


Snapshotting dependencies under RLock is a point-in-time snapshot: a concurrent `Wire` that runs immediately after replaces the field under Lock and the in-flight method keeps the old pointer until it returns. This is accepted because AC-17 requires race-freedom and invariant preservation (not cross-instance coherence during a re-wire), and `Wire` is a boot-time operation in practice. Concurrent re-wiring mid-tick could mix two logistics instances within one operation — outside AC-17's contract.

- **Assumption logged — ASM-1231 (CC, SEC-138 closure).** SEC-138 is closed by defensively copying the `*MissCause` VALUE at each snapshot site while KEEPING the pointer type, rather than migrating `BinStock.MissCause` / `TickerEvent.MissCause` to a value type.

- **ASM-879 (CC fold).** Still-absent edges sit outside the c36778b collaborations gate coverage: engine.farming (tourism/attract/wellbeing consumers), engine.mining (refuse reclamation and four blight-source classes), engine.dispatch and engine.refuse (both to engine.invariant), and engine.refuse to engine.news. The gate enforces only declared-but-unwired edges, so these absent edges will not be auto-caught until declared in master-plan-v2.1.json. BUG-058 is closed; acceptance files cite the gate instead of pending BUG-058.

- **ASM-1020 (FEAT-084 CC fold).** engine.refuse claims MET-G1900-G1999 (codes G1900-G1908 registered in data/errors.json): G1500 crime / G1600 farming / G1700 rail / G1800 education were claimed by parallel sibling modules, so G1900-G1999 was the next free four-digit G block.

- **ASM-1025 (FEAT-084 SF fold).** The section 17 consumption-coefficient-derived waste rate is expressed as data/refuse.json wasteRates per land use because engine.consumption is NOT a registered outbound of engine.refuse (GR#25 flag: engine.refuse -> engine.consumption edge unregistered in code.json); when that edge lands, the rate should be sourced from engine.consumption instead.

### ASM-836 — residual news/invariant edges stay BLOCKED-EDGE; BUG-058 is not the tracker (amends AC-7/AC-12)

BUG-058 is **closed**. Residual: `engine.refuse`→`engine.news` absent; `engine.refuse`→`engine.invariant` absent (AC-12 tripwire already exits 0). AC-7's "ticker names the street" is a `RefuseAPI` ticker-event value, not a news import.

Check: `grep -rn "engine/news\\|engine/invariant" internal/engine/refuse/*.go` (excluding `_test.go`) finds **no** those imports while the tripwires hold. **Tripwire news:** `node -e "const m=require('./code.json').modules.find(x=>x.key==='engine.refuse'); process.exit(m.outbound.calls.some(c=>c.key==='engine.news')?1:0)"` must exit **0**. Invariant tripwire remains AC-12's. **False-pass:** a refuse-local "conserved stock checker" that never registers with `engine.invariant`. **register-guid:** declare collaborations on refuse before live-call prose. **ESCALATE:** mining blight/reclamation and dispatch→invariant are not edited here.

### ASM-1113 — ESCALATE: content-less assumption (title "MOD-039", empty description)

ASM-1113 is filed against `internal/engine/refuse/generate.go` / `engine.refuse` with title **MOD-039** and **empty body**. Nothing to fold into an AC. Identified as refuse (MOD-039) by code.json key and path only. Left for manual triage; this file's ACs are unchanged.

- **ASM-1112 (FEAT-084 CC fold).** Duplicate site registration reuses ErrDisposalSiteUnavailable (MET-G1906, site-domain, carries the site field) over ErrInvalidOverride (round-domain). data/errors.json wording was not updated (file claim held elsewhere); the code stays registry-sourced and the errors.go doc comment carries the rationale. (Roster routed to data.errors; code home is engine.refuse — folded here.)

- **ASM-1116 (FEAT-084 CC fold).** ensureSiteShelf TOCTOU fix holds r.mu (write) across the provisioned-flag check, logistics.Provision, and the provisioned=true flag-set as one critical section; deadlock-safe because engine.logistics is a downstream dependency of engine.refuse that never calls back in, so no r.mu->l.mu->r.mu lock-order cycle exists.
