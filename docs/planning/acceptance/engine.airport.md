
- **ASM-1146 (confirm-and-close).** Air-cargo handoff is the airport-defined `AirCargoMover` dependency-inversion seam (signature carries `freight.Commodity`/`freight.MovementResult`), not a direct `FreightAPI` call; no airport-local tonnage ledger, and until the composition root wires a freight-side adapter, `AirCargo` rejects loudly.
- **ASM-1190 (confirm-and-close).** SEC-118 changes the four `Wire*` methods (`WireFreight`/`WireBlight`/`WirePermit`/`WireSurface`) to return `error` so the copy-guard has a signal — a breaking signature change confined to this package (verified by grep: the only callers are `airport_test.go`; `engine.chemicals/refinery.go` has same-named methods on a different receiver, out of scope).
- **ASM-1149 (confirm-and-close).** Airport data defines a three-tier ladder with a **new `continental_hub` middle rung**: `regional_airport` and `heathrow_class_international_airport` extend the existing `data/buildings.json` catalogue entries (ASM-668), and `continental_hub` is added as placeholder balance data to represent the spec §44 three-rung reach ladder (domestic → continental → global).
