# Findings — Bev consolidation + P0/P1 remediation session (2026-08-25 → 2026-08-26)

Exclusive-control session (Aaron: "you are the only terminal"). Consolidated E:\git and drove the P0/P1 queue. This is the durable write-up of what the destructive rounds found.

## The four dispatched lanes (reviewed and cleaned)

### ASM-1233 — ErrGhostCity collision (P0) — CLOSED
- **Finding:** the actual rename Aaron ruled had **already landed on main** (`e2bfcb6`): projections `ErrGhostCityProviderShape` → `ErrCurveProviderMissingPeak` (MET-G108, message rewritten); spiral keeps `ErrGhostCityNoWarning`/MET-G1102 as the sole ghost-city-death owner. No bare `ErrGhostCity` remains; `add-error.js check` clean. The item was simply never closed.
- **Action:** landed a regression test (`TestASM1233ProviderShapeCodeDistinctFromSpiralGhostCity`, commit `8158f17`, test-only exempt tier) and closed. **Lesson:** BOW status lagged git — the same class as REFRESH #13's "committed but marked open". Audit ref-vs-status on P0s.

### BUG-333 — f2.finance publishes zeros (P1) — CLOSED
- **Finding (r1 REJECT → r2 ACCEPT):** the filed symptom (zeros) was **already fixed by BUG-355**; the old ledger read was lock-safe and published a seeded nonzero on 9cd7fbe. The r1 test could not fail (post-BUG-355 `syncMoneyFromLedger` keeps mirror==ledger at phase boundaries, so old and new code both passed) — the project's recurring "test that cannot fail" class.
- **Action:** r2 rewrote the test to force ledger≠mirror (both assertions RED on revert) + a fixture-degeneracy guard; reframed honestly as consistency-with-chrome hygiene (f2.finance now reads the BUG-324 published treasury mirror, single source). Commit `4b273ba`. **Ruling formalized:** close as fixed-by-BUG-355.

### BUG-362 — watchable-month blockers (P1, FEAT-083 keystone) — BLOCKER #2 LANDED, rest deferred
- **Finding (r1 REJECT → split → r2 ACCEPT):** the render fix (world.SetStructure + build.Tick) is **proven good** — buildings render end-to-end via the real `TileCells` accessor, RED when the call is removed. But the increment **smuggled ~45 lines of out-of-scope dead money-circulation code** into compose.go (`settleCompletedConstruction()` never called, `PostHouseholdSpend` stub) that failed gofmt and needs an Aaron ruling. Classic scope-creep-past-a-verdict.
- **Action:** split — reverted compose.go to trunk, landed only the 2-file render increment (`1519cbc`). Item stays OPEN for deferred blockers #1 (keyboard build path), #3 (money circulation — **needs Aaron ruling:** SettleConstruction breaks budget closure, PostHouseholdSpend lands in an untracked account), #4 (per-citizen wealth), #5 (households inert).

### BUG-375 — determinism gate under-covers (P1) — r3 IN PROGRESS (two REJECTs)
- **THE MATERIAL FINDING of the session.** The determinism gate (GR#21, the line-stopper) has been giving **false confidence on every non-population hook**. r1 caught that the regression test compared different data shapes (header+buf vs buf) so it always "passed". r2 proved the deeper hole: the gate hashes only `{Tick,Month,Seed}` + `PopulationHash` (citizens store only). An attacker injected **conserving** map-order nondeterminism into `financeHook` — same-seed treasury diverged ~54,000 micropounds across 6 runs while PopulationHash stayed byte-identical, and **the gate stayed GREEN**. finance/crime/refuse/ledger nondeterminism ships green today.
- **Action:** r3 fixer building a **broad composed-state digest** (all module observables, deterministically ordered), with the bar that injected non-population-hook nondeterminism must turn the gate RED (proven on two hooks). Fix stays uncommitted until an r3 round ACCEPTs.
- **Lesson worth a Golden-Rules note:** a determinism gate that hashes a narrow projection of state is a gate that cannot fail for everything outside that projection — the "gates that cannot fail" class from the built-but-not-wired memory, applied to GR#21 itself.

## Consolidation outcome
- 3 estates committed + pushed clean (webconsole/BUG-385, BUG-376+285, checkpoint/allowlist) + ASM-1233 + BUG-333 + BUG-362 = **6 commits pushed**, both identity fields noreply on every range (BUG-353 policy held).
- ~27 rescue branches pushed to origin; **23 worktrees removed**, 13 kept (3 active lanes, 8 guard-blocked, bro-dirty, pr52-diverged).
- **Guard bugs surfaced by the salvage sweep:** BUG-386 (guard misses code-bearing commits via `git -C` to another worktree — wrong-repo staged-set resolution; reproduced in-the-wild on 5 rescue commits) and BUG-387 (recency rule self-blocks legitimate second commits — a ref always post-dates its own accept verdict, so every estate needs a fresh round even with zero code change).

## Standing items for Aaron
1. **BUG-369** ACCEPT verdict write — the attacker and lead are both classifier-blocked from recording another agent's verdict; needs your run or a permission rule.
2. **BUG-362 blocker #3** money-circulation design ruling (construction cost sink + household-spend account).
3. **8 guard-blocked trees** (~4,000 lines incl. FEAT-042 protocol impl) — need a `CLAUDE_DISABLE_DESTRUCTIVE_GUARD=1` snapshot session or fresh rounds before they can be removed for a true single-folder finish.
4. The in-game dev-console feedback queue ("DBG-MT9EE2HH, 4 awaiting") is stranded because no Go process talks to the metro DB (ASM-453) — those captures aren't reaching the BOW.
