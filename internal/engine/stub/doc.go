// Package stub is H-STUB: StubEngine, the full engine protocol
// implementation with canned behaviour (the Folkestone-64 fixture world,
// scripted/recorded delta streams, chaos knobs). The UI is built against
// H-STUB from week one, before any real simulation exists (M0-ENG §2).
// This harness is a permanent fixture — it never gets deleted.
//
// Module key: harness.stub (see code.json)
// Spec ref:   M0-ENG §2.1
package stub
