# Build-time git-derived version module (FEAT-1972079872).
# Regenerated on every build/dev by scripts/gen-version.mjs from `git describe`
# + `git log`. NEVER commit it — git is the single source of truth (GR#2).
/src/generated/

# Live version file polled by the running app for HOT version upgrades
# (Aaron, 2026-08-27). Regenerated by the post-commit hook; git is the SSOT.
/version.live.json

# Vite build output
/dist/

# BUG-742 redproof in-tree mutant TEMP (created by the test itself)
/.mutant-tmp/
